name: porlhf
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9  # Assuming the latest Python compatibility as per your setup.py
  - pip=23.3.1
  - numpy
  - tqdm
  - pip:
    - gymnasium
    - imitation
    - .  # This will run 'pip install .' on your local setup.py package